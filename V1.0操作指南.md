# NBA AFTER HOURS V1.0 操作指南

## 启动

推荐双击 `V1.0\NBA After Hours V1.0.exe`。默认中文，界面右上角 **中文 / EN** 可随时切换语言。

第一次可以选择 **自由训练 → 新手训练营**，跟随五个实际操作任务熟悉手感。也可以直接选择球队进入快速比赛。

浏览器版：双击 `Start Game.cmd`，保留启动窗口。默认使用本机 28024 端口，占用时自动尝试后续端口。

## 键盘 P1

| 动作 | 按键 |
| --- | --- |
| 移动 | W / A / S / D；单人模式也可用方向键 |
| 冲刺 | 左 Shift |
| 投篮 | 按住 Space 蓄力，松开出手 |
| 传球 / 防守切人 | J |
| 指定传球 | 数字 1–5 |
| 突破 / 抢断 | K |
| 呼叫掩护 | L |
| 防守盖帽 | Space |
| 切换控制球员 | Tab |
| 暂停 / 继续 | Esc / P，也可以点击右上角暂停按钮 |
| 精彩回放 / 结束回放 | R |
| 切换镜头 | C |
| 静音 | M |
| 全屏 | F；桌面版也支持 F11 |

## 本地双人 P2

在快速比赛中将对手类型选为 **本地双人**，分别选择两队及阵容。

| 动作 | P2 按键 | 小键盘替代 |
| --- | --- | --- |
| 移动 | 方向键 | — |
| 投篮 / 盖帽 | U | 1 |
| 传球 / 防守切人 | I | 2 |
| 突破 / 抢断 | O | 3 |
| 冲刺 | 右 Shift | — |
| 掩护 | H | 5 |
| 切人 | Y | 0 |

只有一个手柄时，它分配给 P2，P1 使用键盘。连接两个标准手柄时，分别控制 P1 与 P2。

## 手柄与触控

标准手柄：左摇杆移动、RT/R2 冲刺、A/Cross 投篮或盖帽、X/Square 传球或切人、B/Circle 突破或抢断、Y/Triangle 掩护、LB 切人、Start 暂停、View/Select 回放。

菜单中可用方向键/摇杆导航，A 确认，B 返回。B 会先关闭当前帮助或设置弹窗，再返回上一层。

触控设备会显示左侧摇杆和右侧动作按钮。按钮文字会随进攻/防守和中英文设置变化；触屏可同时拖动摇杆并按投篮或冲刺。

## 投篮与进攻

1. 找到空间后按住投篮键。
2. 约 **0.70 秒**，让指针进入绿色窗口后松开。
3. 注意体力、距离、防守干扰和球员能力。绿窗提升机会，但并非任何位置都必进。
4. 靠近篮筐会根据球员、距离与时机执行近投或扣篮。
5. 被贴防时可以传球、突破或呼叫掩护。掩护者站定后，内线会顺下，射手可外弹。

连续冲刺会消耗体力。盲目连续抢断会暴露防守位置，适时贴近持球人或封堵出手更有效。

## 比赛、回放与复盘

快速比赛与冠军赛有四节、24 秒进攻计时；打平进入加时。比分、篮板、助攻、抢断、盖帽与命中率会在赛后结算。

进球后按 **R** 播放最近一次精彩回放，比赛计时会停止，再按 R 或点击返回比赛退出。

在暂停页或赛后页点击 **比赛复盘**，可按球队/球员筛选投篮图。圆点为命中，叉号为未进，菱形为被封盖。出手时机数据仅在引擎提供时显示，不会填造 AI 的未知数据。

## 保存、继续与备份

快速比赛与冠军比赛约每 8 秒自动保存，暂停、离开页面和返回菜单时也会尝试保存。主菜单出现 **继续未完比赛** 后，点击可恢复原有比分、计时、球权、球员数据及投篮复盘。恢复后先停在暂停页，按继续比赛上场。

冠军之路还独立保存整个淘汰赛程；每轮结束后的晋级结果不会因刷新而丢失。

设置中的 **备份与恢复** 可以下载 JSON 备份。恢复时选择文件，核对预览，再点击 **导入并刷新游戏**。导入会替换本机长期进度并重新加载游戏；该 JSON 不包含当前中途比赛快照。

桌面版数据在 EXE 旁的 `userdata` 文件夹。整体搬迁桌面存档时，关闭游戏后一起复制 EXE 和该文件夹。浏览器版数据保存在该浏览器当前地址的本地存储中。

## 运行与维护

若画面无法加载，启用浏览器硬件加速或使用桌面版。性能较低时，在设置中将画质切为流畅。音量、音效、音乐、镜头、减少动态效果与操作提示均可调整。

源码开发：`npm ci` → `npm run dev`。验证：`npm test` 与 `npm run test:e2e`。重建桌面包：`npm run package:win`。详细环境说明见 `README.md`。
