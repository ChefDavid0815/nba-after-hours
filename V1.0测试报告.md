# NBA AFTER HOURS V1.0 测试记录

日期：2026-09-17。按你的最新要求停止进一步测试，使用目前已完成的验证结果封包。

## 已完成的验证

| 项目 | 结果 |
| --- | --- |
| 单元、规则与系统测试 | **147 / 147 通过** |
| TypeScript 与生产构建 | 通过 |
| 浏览器完整回归首次运行 | 27 项中 26 项通过；一条固定策略的杯赛获胜用例未赢球 |
| 上述杯赛用例修正 | 仅修正测试起点/种子，未改游戏胜负；连续 **3 / 3 通过**，均以 45–44 真实获胜，正确晋级和刷新续赛 |
| 中途比赛恢复 | 刷新后比分、时钟、球权、球员数据、投篮复盘完全恢复；继续后真实键盘操作有效 |
| JSON 备份界面 | 真实下载、选择文件、预览、导入刷新恢复及错误处理 **3 / 3 通过** |
| 桌面封包内应用 | Electron 报告 `isPackaged=true`、版本 `1.0.0`；实际启动、移动、投篮，无页面错误 |

没有在最后修正杯赛测试后再重跑全部浏览器用例；便携启动器的额外重复验证也按你的要求停止。上述数据区分了已完成和未继续进行的工作。

## 主要覆盖范围

- 投篮、两分/三分、蓄力、绿窗、干扰、篮板、助攻、抢断、盖帽、时钟、压哨与加时。
- 30 队数据、经典阵容、中英文名称、保存格式与异常数据处理。
- 16 队杯赛、实际对手晋级、淘汰结算、重复结算保护与刷新恢复。
- 每日三项挑战、独立纪录、计分与奖牌。
- 双人独立移动/选人/蓄力、键盘＋单手柄分配、标准手柄暂停/返回。
- 真实触控摇杆与投篮、高 DPI 画布尺寸、390×844 / 844×390 / 1440×1000 布局。
- 教学五课真实操作完成、跳过/暂停/中英切换、完成记录保存。
- 回放期间真实时钟冻结、不重复加分；复盘筛选与暂停状态保持。
- 中途引擎快照：15 项专测，18 类中断点后各 120 秒逐帧状态和事件等价；超过 1000 个完整比赛取样点恢复。
- 备份大小/版本/结构验证、危险键拒绝、写入失败回滚与本地存储不可用。

手柄映射使用模拟标准 Gamepad API 验证，触控使用浏览器真实输入协议注入；未逐款连接实体手柄或手机做硬件认证。

## 完整试玩与平衡

第二轮实际计时键盘试玩以 **勇士 31–27 凯尔特人** 结束，四节完整结算，无页面错误。主队 12 次助攻、7 次抢断，双方共 11 次失误。视频、分节截图、统计记录保存在：

`artifacts/playtest-2026-09-17T14-48-54-710Z/`

修复了早期连续抢断造成的大量失误。固定压力实验总失误从约 50 次降到约 11 次；挡拆/空切改动随后通过 144 场策略与防守压力回归。详细数据见 `docs/BALANCE_REPORT.md`。

## 性能测量

当前电脑 Intel Graphics、Chromium、1440×900 视口、设备缩放 1.5；3D 高画质渲染比例上限 1.25。关闭其他测试浏览器后，以单窗口采样：

| 场景 | 平均 FPS |
| --- | ---: |
| 3v3 高画质 | 90.9 |
| 5v5 高画质 | 89.5 |
| 3v3 流畅画质 | 147.2 |
| 5v5 流畅画质 | 148.4 |

这些是该电脑、该测试场景的结果，不代表所有设备。录屏和同时运行多个 3D 测试窗口会降低帧率。

## 记录位置

- `docs/BUILD_LOG.md`：开发与验证记录。
- `docs/BALANCE_REPORT.md`：模拟、战术与平衡实验。
- `docs/QA_MODES.md`：模式流程验证。
- `docs/SAVE_FORMAT.md`：备份文件格式。
- `artifacts/native-packaged/report.json`：封包内应用验证。
- `artifacts/performance/report.json`：单窗口性能结果。
- `tests/`：可重复运行的自动化测试源码。

测试截图与录屏保留在原项目的 `artifacts` 目录，未放入源码 ZIP，以减少发行文件体积。
